# Changelog

התיעוד מתחיל ב-2026-07-25. שינויים קודמים מתועדים ב-`DECISIONS.md` ובהיסטוריית git.

## [לא שוחרר] — ענף `feat/reservation-pricing-engine`

**סטטוס: נבנה ואומת, לא הופעל.** המיגרציות לא הורצו על פרודקשן ולא בוצע דפלוי.
לפני הפעלה יש להריץ את שתי המיגרציות ואז את הדפלוי הקנוני — ראה "הפעלה" בתחתית.

### נוסף

- **מנוע תמחור יחיד** — `lib/pricing/engine.ts`. פונקציה טהורה שהיא מקור האמת היחיד לכל
  סכום בהזמנה: תעריפי לילה, הנחות, מע"מ, יתרה. אחריו לא נותר חישוב מחיר בשום מקום אחר.
- **שלושה מצבי מחיר** — מחיר מקורי (אוטומטי) · מחיר ידני ללילה · סה"כ מחיר ידני.
- **חמישה מצבי הנחה** — מחיר מלא · סכום ללילה · אחוז ללילה · סכום להזמנה · אחוז להזמנה.
- **הנחות לפי אורך שהות (LOS)** — טבלה חדשה `rate_plan_los_discounts` המאפשרת כמה מדרגות
  לאותה תוכנית (7+ ו-28+ יחד). חל **כלל אחד בלבד** — הגבוה מבין המתקיימים — והכלל שהופעל
  מוצג כ-badge ליד בוחר התוכנית.
- **סדר קדימות לתעריף לילה** — `rate_override` → מדרגת LOS → תוכנית תעריפים → מחיר בסיס
  של סוג החדר. עד כה זרימת ההזמנה התעלמה לחלוטין מ-`rate_overrides` (6 שורות קיימות).
- **טוגל "המחיר כולל מע"מ"** — ברירת מחדל דלוקה. השורה מציינת "כלול" או "בנוסף".
- **בורר מטבע מההגדרות** — `tenants.enabled_currencies` במקום קבוע בקוד.
- **פירוט לילי** — expander המציג לכל לילה את התאריך, התעריף ומקורו.
- **שדות כרטיס אשראי בעריכה** — לא היו קיימים כלל; כעת ניתנים להקלדה, זהים לטופס היצירה.
- **תשתית בדיקות** — vitest, 56 בדיקות. הפרויקט לא החזיק בדיקות אוטומטיות כלל.
- `CHANGELOG.md` (קובץ זה).

### תוקן

- **מחיר נשמר בעריכה ללא חישוב מחדש.** עריכת תעריף חדר או תאריכים לא עדכנה את הסכומים —
  הסטור טען מספרים והקפיא אותם. כעת העריכה מחשבת מחדש דרך המנוע, והשרת הוא הסמכות.
- **פרטי כרטיס אשראי לא נשמרו באף הזמנה.** ששה שדות נאספו בטופס היצירה, נשלחו ל-server
  action, ונזרקו לפני ה-INSERT. לא הייתה להם עמודה. התסמין דווח כייחודי להזמנות ערוץ —
  בפועל הוא היה גורף. `isExternal`/`SmartField` לא היה הגורם.
- **הנחה באחוזים אבדה בשמירה.** `discount_per_night` נשמר כ-0 בכל הנחת אחוזים, ולא עודכן
  לעולם אחרי היצירה. כעת נגזר מ-`discountTotal` בכל מצב הנחה.
- **היתרה ב-preview חלקה על כל תצוגה שאחרי השמירה.** טופס היצירה ניכה גם `amountPaid` וגם
  `deposit`, בעוד העמודה ה-GENERATED מנכה `total_paid` בלבד. מקדמה היא תשלום ואינה מנוכה
  פעמיים.
- **יתרת זכות הוצגה כ-0.** יתרה שלילית נחתכה ב-`Math.max(0, ...)`; כעת מוצגת כ"יתרת זכות".
- **תוכנית תעריפים נשמרה ולא השפיעה.** `ratePlanId` לא הוצג בשום UI, לא נצרך בשרת, ולא
  הייתה לו עמודה.
- **פערי סינון `tenant_id`** בארבע שאילתות ב-`reservation-detail.ts` שנשענו על בדיקה מוקדמת.
- **`subtotal`, `discount_per_night` ו-`currency` לא נכללו ב-UPDATE** ולכן התיישנו אחרי עריכה.

### אבטחה

- **מספר כרטיס מלא הוסר מזיכרון הדפדפן.** `CardNumberInput` החזיק PAN בן 16 ספרות ב-state
  של React ומיסך אותו לתצוגה בלבד.
- **נשמרות 4 ספרות אחרונות בלבד.** אין עמודה למספר מלא ואין ל-CVV — גם לא מוצפנים —
  ו-CHECK ברמת ה-DB דוחה כל ערך שאינו 4 ספרות בדיוק.

### שונה

- שיעור המע"מ נשמר **לכל הזמנה** (`reservations.vat_rate`) ולא נקרא עוד מהדייר בזמן חישוב
  מחדש. `tenants.vat_rate` הוא שדה משתנה שכבר שונה 17 → 18; ארבע הזמנות חיות נמכרו ב-17%
  והיו מוצגות מחדש בטעות.

### לא בוצע — ומדוע

- **סעיף 10 (הסרת "הערות חיוב")** — הביטוי אינו קיים בריפו. 0 תוצאות.
- **סעיף 11 (סדר "הערות" מול "מדיניות ביטול")** — אין קומפוננטת מדיניות ביטול לסדר מולה.
- **סעיף 12 (CVC מהערוץ)** — אין מקור נתונים: `channel_booking_revisions` ריקה, ואף אחד
  מ-12 ה-endpoints של Channex אינו endpoint תשלום. מסומן `BLOCKED-EXTERNAL`.

פירוט מלא ב-`DECISIONS.md` וב-`ref/audit/`.

### הפעלה

```bash
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/migrations/2026-07-25_pricing_engine_up.sql
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/migrations/2026-07-25_reservation_card_fields_up.sql
bash /var/www/pms/scripts/deploy.sh feat/reservation-pricing-engine
```

מיגרציית התמחור מאמתת את עצמה ו**נכשלת בכוונה** אם ה-backfill אינו משחזר את
`total_price` ו-`tax_amount` של כל שורה. רולבק: קבצי `*_down.sql` המקבילים.
